package redxax.oxy.remotely.config;

import java.nio.file.Path;

public final class DesktopRemotelyConfigManager extends RemotelyConfigManager {
    public DesktopRemotelyConfigManager(Path applicationDir) {
        super(applicationDir);
    }
}
